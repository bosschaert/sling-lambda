name: Render Page
on:
  workflow_dispatch:

jobs:
  render:
    runs-on: ubuntu-latest
    container: bosschaert/sling-mini-offline:1
    steps:
      - run: |
          java -version && whoami && /root/launch-offline-bare.sh
        name: Render